function printBoard(board:number[][]){
    for(let i = 0; i < 8; i++){
        console.log(String(board[i]));
    }
}

function generate_permutation(){
    let permutation:number[][] = []
    let bad_inputs:number[] = [0,1,2,3,4,5,6,7];
    for(let i = 0; i < 8; i++){
        
    }
}